import Loader from './Loader'

const MainLoader = () => {
    return (
        <div className="min-h-full min-w-full flex items-center justify-center p-5">
            <Loader />
        </div>)
}

export default MainLoader